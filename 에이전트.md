# 숫자의 끝을 보는 타이쿤 인수인계 문서

작성일: 2026-09-04
프로젝트: the-number-factory
현재 단계: 베이스, 제곱, 제곱돌파, 제곱 수렴, 일반화 연구, 제곱 차원 프로토타입

이 문서는 다음 에이전트가 저장소를 처음 열었을 때 게임의 규칙, 현재 구현 상태, 저장 데이터, 코드 구조, 앞으로의 확장 방법을 파악하기 위한 문서다. 아래에서 "현재 동작"이라고 적은 내용은 기획 의도보다 실제 코드가 우선한다.

## 1. 프로젝트 개요

이 게임은 숫자를 직접 키운 뒤 더 높은 연산 챕터로 넘어가는 방치형/프레스티지형 게임이다. 장기 로드맵은 다음과 같다.

| 챕터 | 내부 ID | 상태 |
| --- | --- | --- |
| 증가와 곱셈 | multiplication | 구현 |
| 제곱 | square | 프로토타입 |
| 제곱돌파 | square-breakthrough | 구현 |
| 제곱 수렴 | square-convergence | 구현 |
| 테트레이션 | tetration | 코드 골격만 있고 임시 잠금 |
| 펜테이션 | pentation | 예정 |
| 헥세이션 | hexation | 예정 |
| 하이퍼 연산 | hyper-operation | 예정 |
| 그레이엄수 | graham | 예정 |
| TREE | tree | 예정 |
| SSCG | sscg | 예정 |
| BB | busy-beaver | 예정 |
| 라요수 | rayo | 예정 |

현재는 번들러나 프레임워크가 없는 정적 브라우저 앱이다. 진입점은 저장소 루트의 index.html이며, 일반 script 태그가 순서대로 파일을 불러온다. npm 프로젝트가 아니고 별도 빌드 단계도 없다.

현재 원격 저장소는 https://github.com/killmkill97/the-number-factory 이다. 기본 브랜치 루트에 index.html이 있으므로 GitHub Pages에서 루트 폴더를 배포 대상으로 사용하면 된다.

## 2. 파일 구조와 책임

### 루트

- index.html
  - 모든 화면 패널과 버튼의 HTML이다.
  - CSS와 JavaScript를 로드한다.
  - 실제 진입점은 예전 파일명 number-tycoon.html이 아니라 index.html이다.
- README.md
  - 짧은 프로젝트 소개와 한국어만 제공된다는 안내가 있다.
- LICENSE
  - 저장소 라이선스 파일이다.
- 에이전트.md
  - 이 인수인계 문서다.

### 스타일

styles/number-tycoon.css가 전체 색상과 레이아웃을 담당한다.

- 베이스는 어두운 배경과 황색 강조를 사용한다.
- 제곱 계열은 보라색, 테트레이션 계열은 녹색을 사용한다.
- hidden은 화면에서 숨기는 공통 클래스다.
- 업그레이드를 산 상태는 bought의 녹색 테두리로 표시한다.
- 자동 업글과 자동 LSP 변환기의 토글 상태는 toggle-active/toggle-inactive로 표시한다.
- 뉴스 티커는 상단에 고정된 56px 띠다.
- 수동 교환 도크는 왼쪽 아래에 fixed로 고정된다.
- 연구 트리는 고정 높이의 가로 스크롤/드래그 공간이다.
- Automatium은 화면 위에 표시되는 모달 편집기다.
- 긴 숫자와 긴 문장은 부모 영역을 넘지 않도록 word-break와 폭 측정을 사용한다.

### 데이터와 공통 코어

- scripts/data/chapter-roadmap.js
  - 큰 챕터 목록, 내부 ID, 상태를 보관한다.
  - chapterLabelFor가 제곱돌파와 제곱 수렴 같은 파생 챕터 이름도 처리한다.
- scripts/core/constants.js
  - 큰 수 상수, 밸런스 상수, 저장 버전, 저장 키를 둔다.
- scripts/core/number-math.js
  - 정확한 BigInt와 근사 과학적 숫자를 함께 처리하는 핵심 수학 모듈이다.
- scripts/core/state.js
  - 모든 저장 상태와 대부분의 게임 규칙/수식을 보관한다.
  - 전역 script 방식이라 다른 파일에서 상태 변수와 함수를 직접 참조한다.
- scripts/core/dom.js
  - index.html의 주요 요소를 전역 상수로 잡는다.
  - 숫자 formatter와 공통 log 함수를 둔다.
- scripts/core/chapter-ui.js
  - 현재 챕터, 탭, 패널, body 챕터 클래스를 전환한다.
- scripts/core/render.js
  - requestAnimationFrame으로 렌더 요청을 합치고 모든 시스템의 render 함수를 호출한다.
- scripts/core/save.js
  - 저장 JSON 직렬화, 복구, 구버전 호환, 확인 팝업을 담당한다.
- scripts/main.js
  - 최초 챕터를 베이스로 설정하고 첫 렌더를 호출한다.

### 시스템

- scripts/systems/clickers.js
  - 수동 숫자 클릭, 기본 오토 클릭커, 오토 속도, 병렬화, 오토 퍼센트 충전, 클릭당 수 업그레이드.
- scripts/systems/percent-system.js
  - 첫 퍼센트 레인, 퍼센트 사용, 파워, 오토, 충전량, 다음 레인 해금을 처리한다.
- scripts/systems/percent-lanes.js
  - 2번 이후 퍼센트 레인의 독립 상태와 동적 UI를 처리한다. % 오토 10ms 연구 뒤에는 각 레인도 200ms에서 10ms까지 한 단계 더 강화할 수 있다.
- scripts/systems/auto-upgrades.js
  - 자동 업그레이드가 켜져 있을 때 구매 가능한 업그레이드 하나를 무작위로 시도한다.
- scripts/systems/square-upgrades.js
  - 제곱 영구 업그레이드 보드를 동적으로 만들고 구매/토글한다.
- scripts/systems/square-breakthrough.js
  - 제곱돌파 중첩 업그레이드와 효과 표시를 담당한다.
- scripts/systems/square-convergence.js
  - CP 교환 후 화면, 수렴 업그레이드, 일반화 연구 트리를 담당한다.
- scripts/systems/square-dimensions.js
  - 제곱 차원 화면, x/y/z 변 길이/생산량, 2번 차원의 동적 UI를 담당한다.
- scripts/systems/news-ticker.js
  - 뉴스 문장 풀, 뉴스 모드, 티커 애니메이션, 화폐 HUD 크기를 담당한다.
- scripts/systems/automatium.js
  - N 프로그램 저장/편집/실행, ID 표, 예시, 자동 완성, 실행 스케줄러.
- scripts/systems/dev-console.js
  - Shift+Tab 콘솔, 값 지정, 업그레이드 강제 해금/해제를 담당한다.

### 챕터 코드

- scripts/chapters/square.js
  - 최초 제곱 프레스티지, 컷신, 수동 SP 교환 도크.
- scripts/chapters/tetration.js
  - LSP, tetraP, 8차원, LSP 변환기 코드 골격.
  - 현재 TETRATION_TEMPORARILY_LOCKED 때문에 실제 접근과 생산은 막혀 있다.

### N 언어

- scripts/automatium/n-lexer.js
  - N 소스를 토큰으로 분리한다.
- scripts/automatium/n-parser.js
  - 토큰을 AST로 변환한다.
- scripts/automatium/n-runtime.js
  - AST를 제너레이터로 실행한다. 명령 예산, 구매 대기, whiletick 대기를 표현한다.

### 보관 코드

- scripts/archive/square-dimensions.legacy.js
  - 이전 제곱수 차원 실험 코드다.
  - 현재 index.html에서 로드하지 않는다.
  - 이전 코드는 num과 squareExponent를 별도로 사용하고 중복 배율을 4배에서 제한했으므로 현재 제곱 차원에 그대로 재사용하면 안 된다.

scripts/README.md에도 구조 설명이 있으나 일부 문장이 예전 파일명 number-tycoon.html을 가리킨다. 실제 진입점은 index.html이다.

## 3. 스크립트 로드 순서

전역 변수 의존성 때문에 index.html의 순서는 중요하다.

1. data/chapter-roadmap.js
2. core/constants.js
3. core/number-math.js
4. core/state.js
5. core/dom.js
6. core/chapter-ui.js
7. systems/percent-lanes.js
8. systems/square-upgrades.js
9. systems/square-breakthrough.js
10. systems/square-convergence.js
11. systems/square-dimensions.js
12. core/render.js
13. systems/clickers.js
14. systems/percent-system.js
15. systems/auto-upgrades.js
16. systems/news-ticker.js
17. chapters/square.js
18. chapters/tetration.js
19. core/save.js
20. automatium/n-lexer.js
21. automatium/n-parser.js
22. automatium/n-runtime.js
23. systems/automatium.js
24. systems/dev-console.js
25. main.js

새 파일에서 앞서 선언된 함수/상태를 사용한다면 이 순서를 깨지 않는다. 새 챕터 전용 파일은 보통 render.js가 호출할 수 있도록 render.js보다 앞에 둔다. save.js는 해당 상태가 선언된 뒤에 둔다. 각 URL의 ?v=... 문자열은 브라우저 캐시를 무효화하기 위한 버전 표시다.

## 4. 큰 수 표현과 계산

### 저장 형태

게임 숫자는 작은 동안 정확한 BigInt로 보관한다. 1e33 이상이 되면 다음과 같은 근사 객체로 전환된다.

{ approximate: true, mantissa, exponent }

일반 변수 num에는 호환용 임계값이 들어가고 실제 큰 값은 approximateNum에 있다.

새 코드에서 직접 상태를 조작하지 말고 다음 함수를 사용한다.

- getBaseNumber / setBaseNumber
- addToBaseNumber
- addBaseNumbers
- subtractNumberValues
- multiplyNumberValue
- divideNumberValue
- compareNumberValues
- numberValueFromSave
- serializeNumberValue
- spendBaseCost

SP, CP, 이론, LSP, tetraP, 제곱 차원 값도 가능한 한 NumberMath 값을 사용한다.

### NumberMath API

NumberMath가 제공하는 기능은 다음과 같다.

- isScientific, normalize, normalizeValue
- fromBigInt, fromString, toScientific
- compare, add, subtract, multiply, divide
- min, max, isPositive, isZero
- powerOfTen, power, powerApproximate, log10
- serialize

근사값끼리의 덧셈/뺄셈에서 지수 차이가 12 이상이면 작은 항을 버린다. 이는 성능을 위한 의도된 근사다. 베이스 비용을 뺄 때도 ignoreGap 12를 사용한다. 큰 숫자에서 작은 가격을 구매해도 화면상 값이 거의 변하지 않을 수 있다. 베이스 숫자 생산량은 별도 누적 버퍼 없이 매 생산 틱에 실제 숫자로 바로 합친다. 작은 항을 오래 모았다가 한꺼번에 합치면 화면 숫자가 일정 시간 멈춘 뒤 점프할 수 있으므로 누적 버퍼를 다시 추가하지 않는다.

근사 과학적 숫자에는 음수를 허용하지 않는다. 브라우저 Number로 큰 수를 만들거나 비교하면 정밀도가 깨지므로 새 챕터에서는 반드시 NumberMath를 통과시킨다.

### 표시 규칙

dom.js의 접미사는 다음 순서다.

k, m, b, t, qd, qi, sx, sp, oc, no

지수가 33 미만이면 위 접미사 또는 일반 표기를 사용한다. 지수가 33 이상이면 항상 과학적 표기법을 사용한다. 가수는 한 자리 이상과 최대 약 3개의 유효 숫자로 표시된다. 예: 1.2e42.

제곱 차원은 별도 formatter를 사용해 작은 값도 5e0, 7e-1 같은 과학 표기로 표시한다.

## 5. 현재 게임 흐름

### 베이스 챕터

- 시작 숫자: 0
- 시작 클릭당 증가량: 1
- 클릭당 수 업그레이드 기본 비용: 10
- 클릭당 수 업그레이드는 기본 수를 +1하고 가격을 3배로 만든다.
- 클릭당 기본 수 상한: 256
- 수동 클릭은 effectivePerClick만큼 숫자를 늘린다.
- 퍼센트가 열려 있으면 수동 클릭마다 모든 레인을 1회 충전한다.
- 오토 클릭커 가격: 100, 1회 구매형
- 오토 클릭커 시작 주기: 1000ms
- 한 오토 주기마다 현재 병렬 수만큼 클릭한다.
- 일반 오토 속도: 1000 -> 800 -> 600 -> 400 -> 200 -> 100 -> 50 -> 25ms
- 기본 일반 오토 하한: 25ms
- 제곱돌파 TAS 보유 시 일반 오토 하한: 10ms
- 일반 오토 속도 비용: 200부터 시작해 구매마다 2배
- 병렬화 시작값: 1
- 병렬화는 구매할 때마다 2배, 비용도 2배
- 기본 병렬 상한: 32배
- 제곱 업그레이드 auto_parallel_64 후: 64배
- 제곱돌파 gregtech는 병렬 상한을 레벨마다 2배
- 과거의 수동/오토 확률 배수 보너스는 제거되어 있다.
- gameTick은 게임 속도 업그레이드와 블랙홀/timium을 반영한다.

effectivePerClick의 현재 곱셈 구조:

~~~text
perClick
× 제곱 업그레이드 click_double의 2배
× 수렴 업그레이드 galaxium의 2048배
× 제곱돌파 doctor_octopus의 (8 × Dyson 효과)^레벨
× 현재 활성 제곱 차원들의 숫자 생산 배율
~~~

오토 클릭커는 여기에 병렬 클릭 수와 경과한 주기 수를 곱한다.

### 퍼센트 시스템

- 베이스 숫자 1000 이상에서 자동 해금된다.
- restart_percent_unlock이 있으면 제곱 프레스티지 후 바로 해금된다.
- solid_start가 있으면 시작수 200만을 보장하고 해금을 시도한다.
- 기본 충전 요구량: 100회
- 퍼센트 사용: 현재 값에 percentPower%를 더함
- 기본 퍼센트 파워: 1%
- 기본 충전 감소: 구매마다 4
- percent_charge_minus_16 보유 시 충전 감소: 16
- 기본 최소 충전량: 2회
- overcharge 보유 시 최소 충전량: 1회
- 충전 감소 비용: 250부터 시작하며 구매마다 (현재 비용 × 6 + 4) / 5
- 충전 요구량 50 이하에서 퍼센트 파워 업그레이드가 보인다.
- 기본 퍼센트 오토 가격: 150
- 퍼센트 오토 속도: 1000 -> 800 -> 600 -> 400 -> 200ms
- percent_auto_125 보유 시 퍼센트 오토 하한: 10ms
- 퍼센트 오토 속도 비용: 250부터 시작해 구매마다 2배
- 오토 클릭 퍼센트 충전 가격: 20000
- 오토 클릭 퍼센트 충전은 별도 구매해야 한다.

퍼센트 파워 가격 공식:

~~~text
nextPower = 현재 파워 + 1
standardExponent = nextPower - 2
accelerationStart = 8 + 2 × Dyson효과 × 병목 추적기 레벨
acceleratedLevels = max(0, nextPower - accelerationStart)
acceleratedExponent = acceleratedLevels × (acceleratedLevels + 1) / 2
rawCost = 40000 × 10^(standardExponent + acceleratedExponent)
조정 비용 = rawCost에 디플레이션/모든 비용 감소 효과를 적용한 값
퍼센트 파워 최종 비용 = 조정 비용 < 1e500이면 조정 비용
최초로 조정 비용 >= 1e500이 되면 다음 비용을 조정 비용^p로 만든다.
그 이후에는 매 구매마다 직전 퍼센트 파워 비용^p를 다음 비용으로 사용한다.
여기서 `p = 1.5`이며, 일반화 연구 `3-3`을 연구하면 `p = 1.1`이 된다.
~~~

`1e500` 기준은 다이슨 스웜 수치나 가격 가속 시작점이 아니라 퍼센트 파워의 최종 조정 비용 자체로 판단한다. 다른 베이스 비용에는 이 1.5제곱을 적용하지 않는다.

퍼센트 파워 레벨은 100을 넘겨 계속 올릴 수 있지만 실제 퍼센트 획득 배율은
`min(현재 파워 레벨, 100)`으로 계산한다. 100 이후 구매는 소프트캡과 정체선만 확장한다.

퍼센트 소프트캡:

~~~text
softcap = 1e12 × 10^(percentPower - 1 + 4 × Dyson효과 × overclock레벨)
~~~

소프트캡 미만의 기본 이득은 현재 숫자 × 파워 / 100이다. 기본 퍼센트 파워는 +100%에서 멈추지만, 그 뒤에 제곱 차원 배율과 퍼센트 효율은 정상적으로 적용된다. 소프트캡에 가까워질수록 소프트캡까지 남은 비율을 네 번 연속 곱해 증가량을 감쇠하며, 최종 이득은 소프트캡까지 남은 양을 넘지 않는다. `percent_power_8` 보유 시 감쇠 반복 횟수가 4회에서 3회가 된다. 소프트캡 이상에서는 퍼센트 이득이 0이다.

병목 추적기의 현재 효과는 다음 두 부분이다.

- 퍼센트 가격 가속이 시작되는 파워 레벨을 레벨당 2단계 늦춘다.
- 퍼센트 효율을 4 + (연구 1-1이면 2, 아니면 1) × Dyson효과 × 병목레벨을 4로 나눈 값으로 만든다.

### 퍼센트 레인

퍼센트 레인은 각각 독립적으로 충전되고 사용된다. 같은 자동 처리 틱에 여러 레인이 준비되어 있으면 각 레인이 차례로 현재 숫자에 적용되므로 병렬화의 복리 성장성은 유지된다. 각 레인의 기본 퍼센트 파워에만 +100% 상한이 있으며, 병목 효율과 제곱 차원 배율에는 별도 상한을 두지 않는다. 자동 처리 틱 전체에도 합산 제한을 두지 않는다.

- 기본 레인 수 제한: 8개
- percent_expansion 레벨마다 제한 +1
- 전체 제한: 16개
- 기본 8개는 구매 가능한 공간이고, 레인 확장 업그레이드로 9번 이후를 연다.
- 각 추가 레인은 충전, 파워, 오토, 오토 속도를 독립적으로 가진다.
- 추가 레인 비용은 1번 비용을 scaleByLane으로 약 1.5배씩 키운다.
- 2번 이후 레인은 extraPercentLanes 배열과 동적 UI로 관리한다.
- 오토클리커의 쿨다운은 충전 중에도 진행한다. 따라서 충전이 끝난 시점에 쿨다운이 준비되어 있으면 즉시 %를 사용하며, 실제 사용 뒤에만 타이머를 초기화한다.
- restart_percent_lane2가 있으면 제곱 프레스티지 후 2번 레인을 만든다.

### 첫 제곱 프레스티지

- gameEndValue는 현재 항상 LONG_MAX = 9223372036854775807이다.
- 베이스 숫자가 이 값에 도달하면 checkOverflow가 프레스티지를 준비한다.
- skip_cutscene이 없으면 대화 다섯 줄과 마지막 프레스티지 버튼을 표시한다.
- skip_cutscene이 있으면 컷신 없이 즉시 처리한다.
- 처음 일반수에서 제곱으로 넘어갈 때 기본 보상은 정확히 1 SP다.
- 이후 제곱 탭의 수동 교환은 현재 값/요구량의 로그 공식으로 SP를 계산한다.
- N의 square/sp는 최초 일반수 -> 제곱 진입만 돕고 이후 CP/tetraP/다음 챕터 교환은 하지 않는다.

SP 보상 공식:

~~~text
requirement = 연구 2-1이 없으면 LONG_MAX, 있으면 8e18
ratio = 현재 베이스 숫자 / requirement
logReward = max(log10(ratio), 1)
SP = 기본 SP 획득량 × logReward
~~~

기본 SP 획득량에는 제곱 업그레이드 sp_gain_double과 제곱돌파 extra_investment가 반영된다.

## 6. 제곱 업그레이드

제곱 탭의 업그레이드는 모두 1회 구매형이다. 가격은 SP이며, 제곱 수렴 시 전부 초기화된다. 제곱돌파 탭은 모든 제곱 업그레이드를 구매한 뒤 열린다.

| ID | 이름 | 가격 | 현재 효과 |
| --- | --- | ---: | --- |
| click_double | 클릭 수 2배 | 1 SP | 클릭과 일반 오토 기본 증가량 2배 |
| percent_power_8 | 퍼센트 효율 개선 | 2 SP | 소프트캡 이후 감쇠 4회 -> 3회 |
| percent_auto_125 | % 오토 10ms | 1 SP | 퍼센트 오토 하한 200ms -> 10ms |
| percent_charge_minus_16 | % 요구량 -16 | 1 SP | 충전 감소량 4 -> 16 |
| all_cost_half | 모든 비용 0.5배 | 2 SP | 베이스 숫자 비용 절반 |
| auto_parallel_64 | 오토 병렬 64배 | 1 SP | 병렬 상한 32 -> 64 |
| game_speed_1_5 | 게임 속도 1.5배 | 1 SP | 베이스 타이머 1.5배 |
| sp_gain_double | SP 획득량 2배 | 4 SP | 기본 SP 보상 2배 |
| auto_upgrade_top_down | 자동 업그레이드 | 1 SP | 구매 가능한 베이스 업그레이드 하나를 무작위 구매 |
| skip_cutscene | 컷신 스킵 | 1 SP | 종료수 도달 시 컷신 생략 |
| restart_percent_unlock | 재시작 % 해금 | 2 SP | 프레스티지 후 퍼센트 자동 해금 |
| restart_auto_4x | 재시작 오토 4배 | 1 SP | 재시작 시 오토와 4배 병렬 시작 |
| restart_auto_percent_fill | 재시작 % 채우기 | 1 SP | 재시작 시 오토의 퍼센트 충전 시작 |
| restart_start_100k | 재시작 시작수 10만 | 4 SP | 재시작 숫자 100000 |
| restart_percent_lane2 | 재시작 2라인 | 2 SP | 재시작 시 2번 레인 생성 |

auto_upgrade_top_down은 구매 후 다시 누르면 ON/OFF가 바뀐다. 이름은 예전 순차 구매를 암시하지만 실제 구현은 매번 선택지를 섞어 아무거나 구매하는 방식이다. 자동 업그레이드 시도는 250ms 단위이며 invisible_hand가 시도 속도를 빠르게 한다.

automatium을 구매하면 skip_cutscene과 auto_upgrade_top_down을 내부적으로 구매 완료 처리한다. 자동 업글 작동 플래그는 켜지지 않는다. 따라서 현재 자동으로 활성화되는 동작은 컷신 스킵뿐이고 자동 업글은 사용자가 카드를 눌러 ON으로 해야 한다.

## 7. 제곱돌파

제곱돌파는 squareUnlocked이고 allSquareUpgradesBought일 때 탭이 열린다. 실제 탭에 들어가면 squareBreakthroughEntered가 true가 된다. 제곱돌파 업그레이드는 SP를 소비하며 CP를 얻을 때 모두 초기화된다.

별표(*)가 붙은 효과는 Dyson 효과를 받는다. Dyson 레벨이 d이면 dysonEffectMultiplier = 2^d이다. 중첩 업그레이드의 기존 성장 공식은 `(base × Dyson)^level`이며, 숫자 및 SP 성장 밸런스를 유지하기 위해 이 계산을 바꾸지 않는다.

| ID | 이름 | 기본 가격 | 가격 증가 | 기본 최대 | 효과 |
| --- | --- | ---: | --- | ---: | --- |
| black_hole | 블랙홀 * | 8 SP | 없음 | 1 | 게임 시간 2배, Dyson 적용 |
| extra_investment | 추가투자 * | 8 SP | 매번 4배 | 8 | SP 획득량 2배씩 증가 |
| tas | TAS | 4 SP | 없음 | 1 | 일반 오토 하한 10ms |
| invisible_hand | 보이지 않는 손 * | 2 SP | 매번 1.2배 | 8 | 자동 업글 속도 1.5배씩 증가 |
| overclock | 오버클럭 * | 8 SP | 매번 1.75배 | 8 | 퍼센트 소프트캡 4단계씩 확장 |
| deflation | 디플레이션 | 16 SP | 없음 | 1 | 베이스 비용 0.1배 |
| doctor_octopus | 닥터옥토퍼스 * | 16 SP | 매번 2배 | 8 | 클릭 획득량 8배씩 증가 |
| solid_start | 탄탄한 시작 | 32 SP | 없음 | 1 | 시작수 2000000, 퍼센트 해금 시도 |
| overcharge | 과충전 | 16 SP | 없음 | 1 | 퍼센트 요구량 최소/고정 1 |
| gregtech | 그렉텍 * | 8 SP | 매번 1.5배 | 8 | 오토 병렬 상한 2배씩 증가 |
| bottleneck_tracker | 병목 추적기 * | 64 SP | 매번 10배 | 8 | 퍼센트 가격 가속 지연과 효율 증가 |
| percent_expansion | 퍼센트 확장 | 4 SP | 매번 2배 | 8 | 퍼센트 레인 제한 +1 |
| dyson_swarm | 다이슨 스웜 | 1000 SP | 매번 20배 | 8 | 별표 효과를 레벨마다 2배 |

연구 2-3이 완료되면 Dyson 최대치는 12가 된다. 연구 2-4가 완료되면 병목 추적기 최대치는 16이 된다. 나머지 중첩 업그레이드는 현재 최대 8이다.

중첩 가격은 다음과 같이 계산한다.

~~~text
ceil(baseCost × numerator^level / denominator^level)
~~~

1.2배는 6/5, 1.75배는 7/4, 1.5배는 3/2로 저장되어 있다. all_cost_half와 deflation은 SP 가격이 아니라 베이스 숫자 비용에만 적용된다.

주의: solid_start의 설명에는 기획상 "모든 퍼센트 라인 해금"이라는 의미가 있었지만 현재 코드는 시작수 200만을 보장하고 퍼센트 시스템을 해금한 뒤 영구 레인을 동기화한다. 기본 8개 레인을 무료로 모두 생성하지는 않는다. 기획대로 바꾸려면 applySquareBreakthroughSideEffects와 syncPermanentPercentLanes를 함께 확인해야 한다.

## 8. 제곱 수렴과 수렴 업그레이드

CP 교환은 수동으로만 진행한다.

- 기본 CP 요구량: 1e26 SP, 표시상 100sp
- 일반화 연구 2-2 완료 후: 9e25 SP, 표시상 90sp

CP 보상 공식:

~~~text
ratio = 현재 SP / 수렴 요구량
CP = max(log10(ratio), 1)
~~~

수동 수렴을 실행하면 가능한 CP를 한 번에 계산하고 SP는 0이 된다. 다음이 초기화된다.

- 베이스 런 숫자와 베이스 업그레이드
- 제곱 업그레이드 전체
- 제곱돌파 레벨 전체
- 제곱 차원 두 슬롯의 x/y, 성장 레벨, 비용, 생산 주기, 강화
- 자동 업그레이드 상태와 타이머

다음은 유지된다.

- CP
- 제곱 수렴 업그레이드
- 일반화 연구와 이론
- 저장된 Automatium 프로그램
- 테트레이션 자원 자체는 코드상 유지되지만 현재 효과는 잠겨 있다.

현재 수렴 업그레이드는 모두 1 CP다.

| ID | 이름 | 효과 |
| --- | --- | --- |
| timium | timium | 게임 전체 시간 흐름 2048배 |
| galaxium | galaxium | 클릭/오토 베이스 숫자 2048배 |
| automatium | automatium | N 프로그램 자동화 사용 |
| cp_gain_5x | CP 획득량 5배 | 레벨마다 CP 획득량 5배, 5 CP 시작, 구매마다 가격 5배, 최대 8레벨 |

구매한 수렴 업그레이드는 녹색 테두리로 표시된다. automatium 카드는 구매 후 다시 누르면 N 편집기를 연다.

왼쪽 아래 manualExchangeDock에는 다음 버튼이 있다.

- 제곱 포인트 교환
- 수렴 포인트 교환
- 테트레이션이 실제로 열렸을 때만 보이는 tetraP 교환

일반수에서 최초 제곱으로 넘어가는 과정 외의 SP/CP/tetraP 교환과 다음 챕터 진입은 수동으로 유지하는 것이 현재 정책이다.

## 9. 일반화 연구와 이론

일반화는 제곱 수렴 화면의 일반화 탭이다. 연구 트리는 직육면체 노드와 SVG 선으로 연결된다. 연구 ID의 첫 숫자는 열, 두 번째 숫자는 해당 열의 행이다. 트리 내부에서 우클릭 드래그로 이동할 수 있다.

이론을 연구할 때 SP 또는 CP 중 하나를 버튼으로 선택한다. 두 자원을 동시에 요구하거나 자동 로테이션하지 않는다. 선택한 자원으로 연구할 때마다 이론이 1 증가하고 선택한 자원의 다음 비용만 상승한다.

| 자원 | 시작 비용 | 상승폭 |
| --- | ---: | --- |
| SP | 1e9, 1b | ×10 |
| CP | 1 | ×2 |

연구 구매는 이론을 사용한다. 모든 부모 연구가 완료되어야 하고 이론이 연구 비용 이상이어야 한다. 연구와 이론은 CP 수렴에서 초기화되지 않는다.

| ID | 연구 | 부모 | 이론 비용 | 효과 |
| --- | --- | --- | ---: | --- |
| 1-1 | 병목 추적기 효율 강화 | 없음 | 1 | 병목 추적기 퍼센트 효율 증가량 2배 |
| 2-1 | 제곱 교환 효율 | 1-1 | 2 | SP 교환 요구량 LONG_MAX -> 8e18 |
| 2-2 | 수렴 교환 효율 | 1-1 | 2 | CP 요구량 100sp -> 90sp |
| 2-3 | 다이슨 스웜 허용치 확장 | 1-1 | 4 | Dyson 최대 8 -> 12 |
| 2-4 | 병목 추적기 허용치 확장 | 1-1 | 2 | 병목 추적기 최대 8 -> 16 |
| 3-1 | 제곱 차원 | 2-1 | 3 | 제곱 차원 화면과 생산 해금 |
| 3-2 | 발산자 | 2-2 | 6 | 해금만 되고 현재 기능 없음 |
| 3-3 | 퍼센트 가격 감쇠 | 2-4 | 6 | e500 이후 퍼센트 가격 거듭제곱 1.5 -> 1.1 |
| 4-1 | 제곱 차원 성장 공식 개선 | 3-1 | 3 | 변 성장 log10 -> log9 |
| 4-2 | 제곱 차원 확장 | 3-1 | 6 | 차원 2개, 2번 차원 업그레이드 2배 가격 |
| 5-1 | 차원 일반화 | 4-1, 4-2 | 8 | 제곱 차원에 z 변 추가, 제곱력의 숫자 생산 기여를 제곱 -> 3제곱 |
| 6-1 | 제곱력 기여 제곱화 | 5-1 | 12 | 제곱력이 숫자 생산에 기여하는 배율을 한 번 더 제곱 |
| 6-2 | 제곱력 생산 강화 | 5-1 | 4 | 제곱력 생산 공식을 (x*y*z)^1로 강화하고 기존 SP 강화 구매를 대체 |
| 7-1 | 제곱력 생산 공식 개선 | 6-2 | 4 | 제곱력 생산 공식을 (x*y*z)^3로 강화 |
| 6-4 | 제곱력 소프트캡 강화 | 5-1 | 8 | 제곱력의 지수를 3제곱한 만큼 퍼센트 소프트캡을 확장 |
| 7-3 | 제곱 차원 자동 업그레이드 | 6-1 | 2 | 변, 생산 시간, 생산력 강화 업그레이드를 자동 구매 |

연구를 추가할 때 GENERALIZATION_RESEARCHES에 id, column, row, parents, theoryCost, 설명을 추가하면 UI 노드와 선이 자동 생성된다. 연구 효과는 state.js의 조건 함수에도 연결해야 한다.

## 10. 제곱 차원

제곱 차원은 일반화 연구 3-1 뒤 제곱 화면 안에 나타난다. 연구 4-2 전에는 1개, 이후에는 2개다. 연구 5-1 전에는 x/y만 사용하고, 연구 후에는 각 차원에 z 변이 추가된다.

### 내부 표현

- SQUARE_DIMENSION_SCALE = 100
- 초기 1번 차원 저장값 x=500, y=600, z=700
- 화면 표시: 연구 전 5e0 × 6e0, 연구 후 5e0 × 6e0 × 7e0
- 2번 차원은 같은 기본 x/y/z지만 시작 업그레이드 비용 배율 2
- 각 차원은 x/y/z, 각 변의 비용과 성장 레벨, 생산 주기, 생산 주기 비용, 제곱력 강화 여부를 독립 보관
- 상태 배열은 squareDimensionStates[0], squareDimensionStates[1]

### 변 성장

~~~text
baseGrowth = round(log10(현재 변 길이) × 100) / 100
growthRate = baseGrowth × 해당 변 성장 레벨
~~~

연구 4-1이 있으면 log10(length) / log10(9)를 사용하고 같은 반올림을 한다. x와 y는 100ms 타이머에서 계속 증가한다. 연구 5-1 이후에는 z도 같은 방식으로 증가한다. xGrowthCarry, yGrowthCarry, zGrowthCarry가 소수 누적분을 보관한다.

x/y 버튼은 해당 변 성장 레벨을 +1하고 비용을 2배로 만든다. 연구 5-1 이후 z 버튼도 같은 규칙으로 작동한다. 비용은 SP이며 2번 차원은 처음부터 2배다. 연구 전 z 버튼은 잠긴다.

### 제곱력과 숫자 배율

~~~text
area = x × y
power = area^(1/4)
~~~

연구 5-1 전 제곱력 생산 강화 구매 후 해당 차원의 지수는 1/4에서 1/2가 된다. 연구 5-1 후에는 1/6에서 1/3이 된다. 연구 6-2는 기존 SP 강화 구매를 대체하여 지수를 1로 만들고, 7-1은 지수를 3으로 만든다.

연구 5-1 전 베이스 숫자에 적용되는 차원 배율은 제곱력의 제곱이다. 연구 후에는 부피 x·y·z의 1/6제곱으로 제곱력을 계산하고 제곱력의 3제곱을 배율로 사용한다. 강화 후 지수는 1/3이다. 연구 6-1은 이 최종 차원 배율을 한 번 더 제곱한다.

~~~text
dimensionMultiplier = area^(2 × powerExponent)
연구 5-1 이후: dimensionMultiplier = volume^(3 × powerExponent), volume = x × y × z
연구 5-1 기본: volume^(3 × 1/6) = volume^(1/2)
연구 6-1 이후: dimensionMultiplier = (기존 dimensionMultiplier)^2
연구 6-4 이후: percentSoftcap = 기존 percentSoftcap × 10^(log10(전체 제곱력)^3)
~~~

차원이 2개면 두 차원의 배율을 곱한다. 이 배율은 수동/오토 숫자뿐 아니라 퍼센트 사용으로 얻는 숫자에도 적용된다.

### 제곱력 업그레이드

| 업그레이드 | 1번 차원 가격 | 2번 차원 가격 | 효과 |
| --- | ---: | ---: | --- |
| 제곱력 생산 시간 강화 | 1e26, 100sp | 2배 | 주기를 절반, 최소 1ms, 비용 매번 10배 |
| 제곱력 생산 강화 | 1e20, 100qi | 2배 | 지수 1/4 -> 1/2 |

시간 강화는 ceil(현재 주기 / 2)로 계산한다. 연구 7-3을 완료하면 제곱 차원 화면의 자동 업그레이드가 켜지고, 각 차원의 변 업글, 생산 시간 업글, 제곱력 생산 강화 업글을 순환 구매한다. 이 자동화는 같은 화면의 토글 버튼으로 끌 수 있다. 제곱 차원은 일반 제곱 프레스티지에서는 초기화되지 않고 CP를 얻는 수렴에서만 초기화된다.

현재 제곱 차원의 면적과 성장 계산 일부는 realNumberFromValue를 거쳐 JS Number로 내려온다. 차원 값이 더 커지면 로그 공간 계산으로 바꿔야 한다.

## 11. 테트레이션 현재 상태

테트레이션은 디자인 개편을 위해 임시로 막혀 있다.

~~~js
const TETRATION_TEMPORARILY_LOCKED = true;
~~~

다음 함수는 현재 항상 false다.

- canUnlockTetrationChapter
- canUnlockTetrationDimensions

isTetrationAvailable도 tetrationUnlocked와 잠금 해제를 모두 요구한다. 저장 데이터에 예전 테트레이션 플래그가 있어도 hasTetrationUpgrade와 생산 타이머가 조건을 통과하지 못하므로 효과가 적용되지 않는다.

코드 골격에는 다음이 남아 있다.

- LSP와 tetraP
- 1차원부터 8차원까지의 배열
- 차원 구매 수/비용/생산
- LSP 16384개당 SP 1개 변환
- SP 2000 비용 자동 LSP 변환기와 ON/OFF
- SP가 LONG_MAX 이상일 때 tetraP 정확히 1 획득하는 수동 교환 코드

TETRATION_UPGRADES 배열은 현재 비어 있다. 예전 made_in_heaven 검사 코드는 생산 루프에 남아 있으나 배열이 비어 있고 테트레이션이 잠겨 있어 작동하지 않는다. 예전 매이드 인 해븐과 숫자 은하는 수렴 업그레이드 timium과 galaxium으로 이동/개명되었다.

테트레이션을 다시 열 때는 잠금 상수만 끄지 말고 tetraP 획득 초기화, CP와 tetraP 관계, 저장 데이터 복구 정책, setChapter 잠금, 수동 교환, LSP/SP 변환 상한을 함께 재설계해야 한다.

## 12. Automatium과 N 언어

automatium 수렴 업그레이드를 구매해야 N 프로그램을 실행할 수 있다. 편집기는 모달이며 프로그램 목록과 편집기/문법/업그레이드 ID/프로그램 예시 탭을 가진다.

### 프로그램 저장과 실행

- localStorage 키: numberTycoonAutomatiumPrograms_v1
- 프로그램 최대 개수: 32개
- 프로그램 필드: id, name, source, enabled, updatedAt
- 기본 소스: while true 안의 click
- 이름 최대 길이: 40자
- 코드 저장 또는 Ctrl+S 시 컴파일
- 컴파일 오류 시 프로그램 OFF와 줄 번호 오류 표시
- 목록의 ON/OFF로 실행 토글
- 새 프로그램, 이름 변경, 삭제 지원
- 페이지를 다시 열면 저장된 ON 프로그램을 실행 시도

실행기는 50ms마다 돌며, 모든 프로그램 합계 한 틱 최대 256명령이다. 제너레이터 실행 상태로 구매 대기와 whiletick 대기를 표현한다. 이 제한은 while true가 브라우저를 영구 정지시키지 않게 하기 위해 반드시 유지한다.

N의 click은 수동 숫자 버튼과 같은 동작을 호출한다. 모든 N 프로그램을 합쳐 1초에 최대 한 번만 성공하며, 쿨다운 중 click은 쌓이지 않고 다음 줄로 진행한다.

### 변수와 자료형

문자열은 없다. 자료형은 number와 boolean뿐이다. 선언과 대입은 분리한다.

~~~n
x = number
flag = boolean

x = 10
flag = true
~~~

변수는 선언 전에 대입할 수 없고 자료형을 바꿀 수 없다. 프로그램을 다시 시작하면 실행 컨텍스트가 새로 만들어진다.

### 게임 값

~~~n
get.number
get.sp
get.cp
~~~

- get.number: 베이스 숫자
- get.sp: 제곱 포인트
- get.cp: 수렴 포인트

LSP와 tetraP는 현재 최상위 get 값으로 제공하지 않는다.

업그레이드 조회:

~~~n
get.<종류>.<업그레이드>.<내용>
~~~

지원 내용은 upgraded, amount, cost, max, available이다.

퍼센트 레인 상태는 인덱스를 맨 뒤에 쓴다.

~~~n
get.base.percent_auto.amount[2]
get.base.percent_power.cost[3]
get.base.percent_auto_speed.available[8]
get.base.percent_auto.upgraded[lane]
~~~

레인 표기는 반드시 마지막에 둔다. amout 오타는 amount의 호환 별칭이다.

### 구매

~~~n
buy base click
buy square click_double
buy square_break doctor_octopus
buy square_convergence timium
buy base percent_auto[2]
~~~

종류 별칭:

- base
- square
- square_break, squarebreak, square_breakthrough
- square_convergence, convergence, cp

제곱돌파 별칭:

- dr_octopus -> doctor_octopus
- dyson -> dyson_swarm
- percent_line -> percent_expansion

구매 실패 시 기본적으로 다음 줄로 넘어간다. 끝에 s를 붙이면 구매 가능할 때까지 해당 프로그램만 대기한다.

~~~n
buy square_break doctor_octopus s
~~~

### SP/제곱 명령

~~~n
square
sp
square 10
sp target s
~~~

square와 sp는 같은 명령이다. 최초 일반수에서 제곱으로 넘어갈 때만 의미가 있다. 숫자를 붙이는 옛 호환 문법은 목표 SP 보상을 기다리지만, 이미 제곱이 열린 뒤에는 추가 챕터 교환을 하지 않는다. CP/tetraP 교환과 다음 챕터 진입은 N에서 실행하지 않는다.

### 조건과 반복

~~~n
if get.sp >= 1000 {
    buy square_break dyson_swarm
}
elif get.sp >= 16 {
    buy square_break doctor_octopus
}
else {
    square
}

while true {
    click
}

whiletick 20 get.sp < 1000 {
    buy square_break doctor_octopus
}

for 10 {
    click
}

break
~~~

지원 구문은 if, elif, else, while, whiletick, for, break다. whiletick은 지정한 자동화 틱 수만큼 기다린 뒤 본문을 반복한다. break는 가장 가까운 while/whiletick/for를 빠져나온다.

### 연산자

- 산술: +, -, *, /, %, ^
- 비교: ==, !=, >, >=, <, <=
- 논리: and, or, not
- 괄호: 지원

정수끼리 나누면 정수 나눗셈으로 소수점 이하는 버린다. 근사 큰 수에는 %를 사용할 수 없다. 거듭제곱 지수는 일반 정수이며 0 이상 100000 이하로 제한된다. for 횟수와 whiletick 간격도 근사 큰 수가 아닌 일반 정수여야 한다.

### 주석과 자동 완성

~~~n
// 한 줄 주석

/*
여러 줄 주석
*/
~~~

코드 입력 위치에 따라 명령, get 경로, 업그레이드 ID, 레인 번호, 선언된 변수를 제안한다. 위/아래 화살표로 선택하고 Tab으로 삽입한다. 제안이 없으면 Tab은 공백 4칸을 넣는다.

편집기 탭은 다음과 같다.

- 편집기: 코드, 이름 변경, 삭제, 저장
- N 문법: 현재 구현 문법
- 업그레이드 ID: 종류, 이름, ID, 별칭, 레인 표기
- 프로그램 예시: 기본/제곱/제곱돌파/제곱 수렴 자동 구매 및 목표 SP

새 업그레이드를 추가할 때 N 조회/구매가 필요하면 automatium.js의 ID 가이드, 스냅샷, 별칭, 예시를 모두 갱신한다.

## 13. 개발자 콘솔

Shift+Tab을 누르면 게임 정보 영역 아래에 입력창이 나타난다. 일반 로그 목록은 더 이상 출력하지 않는다. 내부 log 함수는 현재 no-op이다.

지원 명령:

~~~text
set number <숫자>
set sp <숫자>
set cp <숫자>
set lsp <숫자>
set tetraP <숫자>
~~~

일반 정수, 쉼표가 있는 정수, 1e200, 1.2e42 형식을 허용한다.

- set number: 컷신/overflow 정리, 숫자 변경, 퍼센트 해금/overflow 확인
- set sp: 양수이면 제곱 접근을 만들고 제곱 탭으로 이동
- set cp: 양수이면 제곱 수렴 접근을 만들고 수렴 탭으로 이동
- set lsp, set tetraP: 값은 바꾸지만 테트레이션 잠금 때문에 실제 테트레이션 탭은 열리지 않음

콘솔이 열린 동안 업그레이드 카드에서:

- 좌클릭: 비용/선행 조건 무시하고 강제 해금
- Shift+우클릭: 해당 업그레이드 해제

중첩 업그레이드를 해제하면 해당 ID 레벨을 0으로 만든다. 퍼센트 확장, 과충전, TAS처럼 파생 상태가 있는 경우 일부 복구 코드도 실행된다. 새 파생 효과를 추가하면 forceUnlockDevUpgrade와 removeDevUpgrade를 갱신한다.

## 14. 뉴스와 화폐 HUD

뉴스는 상단 고정 띠에서 오른쪽에서 왼쪽으로 이동한다. 긴 문장 폭을 먼저 측정하고 시작/끝 위치와 duration CSS 변수를 계산한 뒤 reflow 후 애니메이션을 시작해 순간이동/깜빡임을 줄인다.

- 속도: 450px/s
- 이전 문장과 같은 문장은 연속 선택하지 않음
- 인간 문장: 43개
- AI 고정 문장: 37개
- AI 조합 확장: 20개 주제 × 15개 행동 = 300개
- AI 모드 풀: 337개

뉴스 설정:

- all: 인간 + AI
- human: 인간 문장만
- ai: AI 문장만
- localStorage 키: numberTycoonNewsMode

오른쪽 위 화폐 HUD에는 숫자, sp, cp, lsp, tetraP, 이론이 항상 보인다. 화폐 HUD 크기는 1.0배부터 4.0배까지 조절하며 키는 numberTycoonCurrencyStatusScale이다.

## 15. 저장/불러오기와 초기화

### 저장

- 게임 저장 키: numberTycoonSave_v1
- 저장 버전: 33
- 저장 버튼 확인 문구: 저장하시겠습니까?
- 불러오기 확인 문구: 불러오시겠습니까?
- 큰 수는 문자열 또는 과학적 문자열로 직렬화한다.
- 타이머와 UI 객체는 저장하지 않고 복구 시 0부터 시작하거나 다시 만든다.

저장 JSON 그룹:

- 베이스: num, perClick, 베이스 비용
- 퍼센트: 해금, 충전, 요구량, 파워, 가격
- 일반 오토: 해금, 속도, 병렬, 퍼센트 충전
- 퍼센트 레인: 레인 수, 다음 비용, 각 레인의 충전/파워/오토/속도
- 챕터: squareUnlocked, squareBreakthroughEntered, tetrationUnlocked, tetrationDimensionsUnlocked, activeChapter
- 포인트: squarePoints, squareConvergencePoints, theory
- 제곱 차원: 호환용 1번 차원 top-level x/y/z 필드와 새 squareDimensions 배열
- 이론 비용: SP/CP 비용과 선택 자원 index
- 구매 상태: 제곱, 제곱돌파, 제곱 수렴, 일반화 연구
- 자동 업글: autoUpgradeEnabled
- 테트레이션: LSP, tetraP, 8차원 배열, 구매 수, 비용, 업그레이드 상태

### 복구 호환

- 버전 5 미만은 legacy save로 본다.
- legacy save는 예전 규칙에서 안전하게 복구하기 위해 일부 상위 진행을 초기화한다.
- 예전 top-level 제곱 차원 필드는 1번 차원으로 읽는다. 예전 저장에 z가 없으면 z=700, z 비용/성장 레벨은 기본값으로 보정한다.
- 새 squareDimensions가 있으면 배열을 우선 읽는다.
- 차원 주기는 1ms~1000ms로 clamp한다.
- 현재 목록에 없는 저장 업그레이드는 적용하지 않는다.
- 테트레이션이 잠겨 있으면 저장 flag가 있어도 현재 챕터를 열지 않는다.
- 제곱돌파 레벨은 현재 최대치로 clamp한다.
- percentPowerUpgradeCost와 추가 레인 파워 비용은 현재 파워로 다시 계산한다.
- 불러온 뒤 동적 퍼센트 레인과 테트레이션 차원 UI를 비우고 재생성한다.

### 초기화 경계

| 동작 | 초기화 | 유지 |
| --- | --- | --- |
| 최초 제곱 프레스티지 | 베이스 런 | SP와 제곱 접근 |
| 제곱 프레스티지 후 재시작 | 베이스 숫자/업그레이드/퍼센트 런 | 제곱 업그레이드/돌파/CP/연구 |
| CP 수동 교환 | 베이스 런, 차원, 제곱 업그레이드, 제곱돌파 | CP, 수렴 업그레이드, 연구, 이론 |
| tetraP 획득 코드 | SP, 제곱 업그레이드/돌파, LSP 생산 상태 등 | tetraP와 상위 영구 상태. 현재 잠금 |

관련 함수:

- resetRunStateAfterSquarePrestige
- collectSquareConvergenceIfReady
- resetSquareDimensionState
- resetSquareUpgradeState
- resetSquareBreakthroughState
- resetAfterTetraPointGain

새 통화를 추가할 때는 어느 프레스티지가 그 통화를 지우는지 먼저 정하고 해당 reset 함수에 명시한다.

## 16. 새 챕터 추가 절차

1. chapter-roadmap.js에 내부 ID, 라벨, 상태를 추가한다.
2. constants.js에 요구량, 비용, 상승폭, 제한 상수를 추가한다.
3. state.js에 영구 자원, 런 자원, 업그레이드 정의, 해금 조건, 획득 공식, 초기화 함수를 추가한다.
4. index.html에 탭과 패널을 추가한다.
5. dom.js에 필요한 요소 참조를 추가한다.
6. chapters/<chapter>.js에 챕터 전환과 수동 교환을 둔다.
7. 재사용 생산/자동화는 systems/ 아래에 둔다.
8. chapter-ui.js에 잠금 조건, 탭 노출, body class, 패널 토글을 연결한다.
9. render.js에 통화/비용/버튼/UI 렌더를 연결한다.
10. save.js에 저장과 복구를 추가하고 SAVE_VERSION을 올린다.
11. 새 통화를 유지할지 지울지 모든 초기화 함수에서 확인한다.
12. 현재 정책대로 새 챕터 교환을 N 자동화에 넣지 않을지 먼저 결정한다.
13. 새 업그레이드가 N에 노출되어야 하면 automatium.js의 ID 표/스냅샷/예시를 갱신한다.
14. script URL의 cache-busting version을 갱신한다.
15. 문법, 저장 복구, 모바일, 큰 수, 수동 교환을 확인한다.

### 수식 작성 규칙

- 큰 수에 Number, parseFloat, 일반 Math.pow를 직접 쓰지 않는다.
- 정수 상수는 BigInt를 사용한다.
- 자원 비교는 compareNumberValues를 사용한다.
- 베이스 숫자 비용은 discountedCost를 거친다.
- 저장은 serializeNumberValue를 사용한다.
- 표시에는 fmtPowerBase 또는 챕터 formatter를 사용한다.
- BigInt와 Number를 직접 혼합하지 않는다.
- 로그/거듭제곱은 NumberMath.log10, power, powerApproximate를 우선 사용한다.
- 근사값에서 작은 항을 버리는 경우 이유와 허용 오차를 기록한다.

### 업그레이드 작성 규칙

정의 배열을 단일 진실 공급원으로 사용한다. 정의에는 ID, 이름, 설명, 비용, 최대 구매 수, 가격 배율, Dyson 영향을 넣을 수 있다. 구매 함수, 렌더, 개발자 콘솔 강제 해금/해제, N ID/스냅샷을 함께 확인한다.

중첩 업그레이드는 구매 횟수와 실제 효과를 분리한다.

- upgraded: 구매 횟수
- amount: 현재 실제 효과
- cost: 다음 단계 비용
- max: 현재 제한
- available: 실제 구매 가능 여부

## 17. 검증 체크리스트

현재 자동 테스트 프레임워크는 없다. 최소한 다음을 수동 또는 브라우저 개발자 도구로 확인한다.

### 기본/프레스티지

- 새 상태에서 +1이 작동한다.
- 숫자 1000에서 퍼센트가 열린다.
- 오토 구매와 속도/병렬 상한이 정상이다.
- LONG_MAX 부근에서 최초 제곱 프레스티지가 한 번만 준비된다.
- 컷신 스킵 유무가 정상이다.
- 제곱 프레스티지 후 베이스만 초기화된다.
- 제곱 업그레이드 전부 구매 전 제곱돌파가 잠긴다.
- CP 교환은 수동 버튼에서만 된다.
- CP 교환 후 SP/제곱 업그레이드/돌파/차원이 초기화된다.

### 숫자/성능

- 1e33 이상이 과학 표기로 바뀐다.
- 1e200과 1e8000이 사이트를 멈추게 하지 않는다.
- 작은 비용 구매 시 큰 수가 거의 변하지 않는 근사가 의도대로다.
- BigInt/Number 혼합 오류가 없다.
- 제곱 차원 값과 생산량이 과학 표기로 보인다.

### 연구/차원

- 연구 1-1은 부모 없이 열린다.
- 부모 하나라도 미완료면 자식 연구가 잠긴다.
- 이론 자원 선택 시 선택한 자원만 소모되고 그 비용만 상승한다.
- 연구 3-1 뒤 제곱 차원 탭이 보인다.
- 연구 4-1 뒤 log9가 적용된다.
- 연구 4-2 뒤 2번 차원과 2배 가격이 보인다.
- 연구 5-1 전에는 x/y 공식이 유지되고 z가 잠긴다. 연구 후 z 변, x·y·z 공식, 제곱력^3 배율이 적용된다.
- 변을 여러 번 구매했을 때 성장률이 성장 레벨에 비례한다.
- CP 뒤 차원이 기본값으로 돌아간다.

### Automatium/N

- 문법 탭의 예제가 컴파일된다.
- while true가 256명령 예산 안에서 브라우저를 얼리지 않는다.
- click이 모든 프로그램 합계 1초에 한 번만 성공한다.
- [2] 레인 인덱스가 맨 뒤 문법으로 동작한다.
- buy ... s가 자원 부족 시 대기한다.
- N 오류 시 줄 번호가 보이고 프로그램이 OFF가 된다.
- ON/OFF와 코드/이름이 새로고침 후 복구된다.
- CP와 tetraP 교환을 N이 자동 실행하지 않는다.

### UI/저장

- 저장/불러오기 확인 팝업이 표시된다.
- 구버전 저장을 오류 없이 복구한다.
- 긴 뉴스 문장이 순간이동하거나 깜빡이지 않는다.
- 뉴스 모드와 화폐 크기가 새로고침 후 유지된다.
- Shift+Tab, 좌클릭 해금, Shift+우클릭 해제가 작동한다.
- 일반 로그가 누적되지 않는다.
- 모바일에서 수동 교환 도크와 Automatium 모달이 화면 밖으로 나가지 않는다.

## 18. 알려진 제한과 다음 우선순위

1. 테트레이션 재설계
   - 현재 임시 잠금이며 업그레이드 배열이 비어 있다.
   - 다음 상위 챕터 전에 tetraP 흐름과 초기화 경계를 확정해야 한다.
2. 제곱 차원 큰 수 최적화
   - 일부 계산이 realNumberFromValue로 JS Number에 내려간다.
   - 값이 커지면 로그 공간 상태로 바꾼다.
3. solid_start 의미 정리
   - 설명과 실제 레인 생성 동작이 다르다.
   - 무료 8개 생성인지 구매 가능 제한인지 확정하고 코드/설명을 맞춘다.
4. 저장 마이그레이션 테스트
   - 실제 과거 저장 파일 샘플을 여러 버전으로 보관한다.
5. 수식 테스트
   - SP/CP 로그, 소프트캡, 가격 가속, Dyson, 제곱 차원 성장률을 고정 입력으로 검사한다.
6. N 기능 확장
   - 현재 get은 number/sp/cp만 제공한다.
   - 챕터 교환 자동화는 수동 전환 정책과 충돌하므로 먼저 기획을 확정한다.
7. 오프라인 진행/자동 저장
   - 현재 없다. 추가 시 저장 빈도와 큰 수 직렬화 비용을 정한다.
8. 로그 UX
   - log는 no-op이므로 개발자 명령 성공/실패도 화면에 표시되지 않는다.
   - 되살릴 경우 일반 게임 로그와 개발자 출력 범위를 분리한다.
9. 동적 DOM 정리
   - 레인/2번 차원/테트레이션 차원은 불러오기 시 기존 UI를 제거하고 재생성하는 패턴을 유지한다.
10. 미래 챕터 구조
   - 펜테이션 이후를 단순 자릿수 확장으로 만들지 말고 자원/표현/교환/초기화/자동화 정책을 챕터별로 독립 정의한다.

## 19. 배포와 작업 마무리

정적 GitHub Pages 배포 작업:

1. git status로 변경 파일을 확인한다.
2. 각 JS에 node --check를 실행하고 git diff --check를 실행한다.
3. 브라우저에서 index.html을 열어 체크리스트를 확인한다.
4. 의도한 파일만 커밋한다.
5. 원격 저장소의 Pages 설정에서 배포 브랜치와 루트 폴더를 선택한다.
6. 루트 index.html이 배포 진입점인지 확인한다.

새 기능 작업이 끝나면 코드만 고치지 말고 이 문서의 표, 수식, 저장 버전, 초기화 경계, 알려진 제한도 갱신한다. 이 게임에서 가장 위험한 회귀는 화면 숫자와 실제 저장/교환/초기화 규칙이 서로 달라지는 경우다.
